---
- name: "Install nginx"
  ansible.builtin.apt:
    name: nginx
    state: present
  tags:
    - nginx

- name: "Ensure nginx is running and enabled"
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true
  tags:
    - nginx